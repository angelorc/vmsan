export default defineNuxtConfig({
  // hooks: {
  //   "imports:extend": (imports) => {
  //     // @nuxt/ui's auto-import scanner incorrectly detects 'options'
  //     // (a function parameter in useResizable.js) as a named export.
  //     // This causes a runtime SyntaxError. Remove it.
  //     for (let i = imports.length - 1; i >= 0; i--) {
  //       if (imports[i].name === "options" && imports[i].from?.includes("useResizable")) {
  //         imports.splice(i, 1);
  //       }
  //     }
  //   },
  // },
});
