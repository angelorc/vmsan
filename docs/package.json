{
  "name": "vmsan",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "docus": "5.7.0",
    "nuxt": "^4.3.1"
  },
  "overrides": {
    "unimport": "5.6.0"
  },
  "notes": {
    "unimport": "Pinned to 5.6.0 to work around unjs/unimport#273 and unjs/mlly#303 — v5.7.0 exposes a mlly bug where function params are misdetected as exports, breaking @nuxt/ui's useResizable. Remove once either issue is fixed."
  }
}
